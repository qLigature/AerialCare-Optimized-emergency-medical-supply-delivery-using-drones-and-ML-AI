<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

  <link rel="stylesheet" href="assets/css/dashboard.css">
  <title>AerialCare - Dashboard</title>
</head>

<body>

  <!-- Main Navbar -->

  <nav id="main-navbar" class="navbar navbar-expand-lg navbar-light bg-white fixed-top">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">
        <h3 class="ms-3 mt-2">AerialCare - Add New Order</h2>
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#sidebarMenu"
        aria-controls="sidebarMenu" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
    </div>
  </nav>

  <!-- Sidebar -->

  <nav id="sidebarMenu" class="sidebar p-3 d-lg-block collapse text-white bg-danger">
    <a href="#"
      class="desktop avatar d-flex align-items-center my-3 py-3 mb-md-0 me-md-auto text-white text-decoration-none">
      <img src="assets/images/profile-user.svg" alt="" width="50" height="50">
      <div class="ms-3">
        <p class="fw-bold">Mr. Firstname Lastname</p>
        <p>fullname@gmail.com</p>
      </div>
    </a>
    <hr class="desktop">
    <a href="./newOrderPage" class="w-100 btn btn-lg bg-white mb-4" type="submit">Add a New Order</a>
    <ul class="nav nav-pills flex-column mb-auto">
      <li class="nav-item">
        <a href="./dashboardPage" class="nav-link text-white" aria-current="page">
          Dashboard
        </a>
      </li>
      <li>
        <a href="./ordersPage" class="nav-link text-white">
          Orders
        </a>
      </li>
      <li>
        <a href="#" class="nav-link text-white">
          Help Center
        </a>
      </li>
      <li>
        <a href="#" class="nav-link text-white">
          Settings
        </a>
      </li>
      <li>
        <a href="./landingPage" class="nav-link text-white">
          Sign Out
        </a>
      </li>
    </ul>
  </nav>

  <!-- Body -->

  <main>

    <div class="dashboard-card">
      <h2 class="pt-2 pb-4">Order Details</h2>
      <form method="">

        {{!-- Add button to trigger geolocation --}}
        <div class="d-flex align-items-center mb-4">
          <button type="button" onclick="getLocation()" class="btn btn-lg border">🔍 Get Location</button>
          <h4 id="demo" class="ms-3 mt-2">No destination detected</h3>
        </div>

        {{!--
        <!-- Add first aid kit + medication category if there's time -->
        <div class="input-group pb-3">
          <label class="input-group-text" for="inputGroupSelect1">Select Product Category</label>
          <select class="form-select" id="inputGroupSelect1">
            <option selected value="card">Blood</option>
            <option value="bank">Organs</option>
          </select>
        </div> --}}

        <!-- Load products programatically here, according to category selected -->
        <div class="input-group pb-3">
          <label class="input-group-text" for="inputGroupSelectProduct">Select Product</label>
          <select class="form-select" id="inputGroupSelectProduct" required>
            <option value="">Products</option>
            <option value="kidney">Kidney</option>
            <option value="eyes">Eyes</option>
            <option value="heart">Heart</option>
            <option value="liver">Liver</option>
            <option value="bone_marrow">Bone Marrow</option>
            <option value="ab_positive_blood">Type AB- Blood</option>
            <option value="ab_negative_blood">Type AB+ Blood</option>
            <option value="o_negative_blood">Type O- Blood</option>
            <option value="o_positive_blood ">Type O+ Blood</option>
            <option value="a_negative_blood">Type A- Blood</option>
            <option value="a_positive_blood">Type A+ Blood</option>
            <option value="b_negative_blood">Type B- Blood</option>
            <option value="b_positive_blood">Type B+ Blood</option>
          </select>
        </div>

        <div class="input-group pb-3">
          <label class="input-group-text" for="inputGroupSelectPayment">Select Payment Method</label>
          <select class="form-select" id="inputGroupSelectPayment">
            <option selected value="card">Credit/Debit Card</option>
            <option value="bank">Internet Banking</option>
            <option value="upi">UPI</option>
          </select>
        </div>

        <div class="form-floating pb-4">
          <input type="text" class="form-control" id="floatingNumber" placeholder="Phone Number" required>
          <label for="floatingNumber">Phone Number</label>
        </div>

        <div class="d-flex justify-content-between pb-4">
          <h3>Total:</h3>
          <h3>₹2,000.00</h3>
        </div>

        <div class="d-flex justify-content-center w-100">
          <button class="btn btn-lg bg-danger text-light mb-4 px-5 ms-auto me-auto" type="submit">Submit Order</button>
        </div>

      </form>

    </div>

  </main>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM"
    crossorigin="anonymous"></script>

  <script>
    const x = document.getElementById("demo");

    function getLocation() {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(showPosition);
      } else {
        x.innerHTML = "Geolocation is not supported by this browser.";
      }
    }

    function showPosition(position) {
      x.innerHTML = position.coords.latitude + ", " + position.coords.longitude;
    }

    document.querySelector('form').addEventListener('submit', async (e) => {
      e.preventDefault();
      const product = document.getElementById('inputGroupSelectProduct').value;
      let coords;

      // Get saved coordinates from demo
      if (x.innerHTML === "No destination detected") {
        alert('Please select a destination');
        return;
      } else {
        const latitude = Number(x.innerHTML.split(',')[0]);
        const longitude = Number(x.innerHTML.split(',')[1]);
        coords = [latitude, longitude];
      }
      const id = await sessionStorage.getItem('user');

      console.log(coords);

      fetch('http://localhost:9001/buy', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
        },
        body: JSON.stringify({
          user_id: id,
          product,
          location: coords,
          note: 'sample note',
        }),
      })
        .then((res) => res.json())
        .then((data) => {

          if (data.status === 'success') {
            alert('Order placed successfully');
            window.location.href = './dashboardPage';
            return;
          }
          alert(data.message);
          return;
        })
        .catch((err) => console.log(err));
    });
  </script>
</body>

</html>